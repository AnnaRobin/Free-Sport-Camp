package com.masterpiece.FreeSportCamp.controllers;

import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.masterpiece.FreeSportCamp.services.MemberService;
import com.masterpiece.FreeSportCamp.services.SportService;

@RestController
@RequestMapping("/sports")
@CrossOrigin(origins = "")
public class SportController {

	/*private final SportService service;
	
	protected SportController(SportService service) {
		this.service = service;
	}
	*/

}
